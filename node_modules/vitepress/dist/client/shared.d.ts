import { AdditionalConfig, AdditionalConfigDict, AdditionalConfigLoader, Awaitable, CodeCopyButtonOptions, ContainerOptions, DefaultTheme, HeadConfig, HeadConfig as HeadConfig$1, Header, LocaleConfig, LocaleSpecificConfig, MarkdownEnv, MarkdownLocaleOptions, PageData, PageData as PageData$1, PageDataPayload, Route, SSGContext, SiteData, SiteData as SiteData$1, VitePressData } from "../../types/shared.js";
//#region src/client/shared.d.ts
declare const EXTERNAL_URL_RE: RegExp;
declare const APPEARANCE_KEY = "vitepress-theme-appearance";
/**
 * Parses a fully qualified `collection:name` icon name, corresponding to
 * the `vpi-<collection>-<name>` class. Returns null for anything else,
 * keeping malformed input out of generated selectors and class attributes.
 */
declare function parseIconName(name: string): {
  collection: string;
  icon: string;
} | null;
/**
 * Placeholder prepended to SSR-emitted URLs when base is relative, later
 * replaced with each page's `../` prefix back to the site root.
 */
declare const RELATIVE_BASE_SENTINEL = "/__VP_BASE__/";
declare function isRelativeBase(base: string): boolean;
/**
 * The ../-prefix that leads from `relativePath`'s directory back to the
 * site root ('./' for root-level pages).
 */
declare function relativePathToRoot(relativePath: string): string;
/**
 * Join two paths, collapsing slash collisions but keeping the `//` that
 * follows a protocol.
 */
declare function joinPath(base: string, path: string): string;
declare const VP_SOURCE_KEY = "[VP_SOURCE]";
declare const inBrowser: boolean;
declare const notFoundPageData: PageData$1;
declare function isActive(currentPath: string, currentHash: string, matchPath: string, asRegex?: boolean, skipHashCheck?: boolean): boolean;
declare function normalize(path: string): string;
declare function isExternal(path: string): boolean;
declare function getLocaleForPath(siteData: SiteData$1 | undefined, relativePath: string): string;
/**
 * Resolves the site data for a route, layering the matched locale and
 * additional configs over the root config.
 */
declare function resolveSiteDataByRoute(siteData: SiteData$1, relativePath: string, filePath?: string): SiteData$1;
/**
 * Create the page title string based on config.
 */
declare function createTitle(siteData: SiteData$1, pageData: PageData$1): string;
declare function mergeHead(...headArrays: HeadConfig$1[][]): HeadConfig$1[];
declare function sanitizeFileName(name: string): string;
declare function slash(p: string): string;
declare function treatAsHtml(filename: string): boolean;
declare function escapeRegExp(str: string): string;
/**
 * @internal
 */
declare function escapeHtml(str: string): string;
/**
 * Creates a readonly proxy behaving like a deep merge of the given layers,
 * without mutating them. Earlier layers take precedence.
 */
declare function stackView<T extends ObjectType>(..._layers: Partial<T>[]): T;
declare namespace stackView {
  var unpack: <T>(obj: T) => T[] | undefined;
}
type ObjectType = Record<PropertyKey, any>;
declare function isObject(value: unknown): value is ObjectType;
declare function isShell(lang: string): boolean;
//#endregion
export { APPEARANCE_KEY, type AdditionalConfig, type AdditionalConfigDict, type AdditionalConfigLoader, type Awaitable, type CodeCopyButtonOptions, type ContainerOptions, type DefaultTheme, EXTERNAL_URL_RE, type HeadConfig, type Header, type LocaleConfig, type LocaleSpecificConfig, type MarkdownEnv, type MarkdownLocaleOptions, type PageData, type PageDataPayload, RELATIVE_BASE_SENTINEL, type Route, type SSGContext, type SiteData, VP_SOURCE_KEY, type VitePressData, createTitle, escapeHtml, escapeRegExp, getLocaleForPath, inBrowser, isActive, isExternal, isObject, isRelativeBase, isShell, joinPath, mergeHead, normalize, notFoundPageData, parseIconName, relativePathToRoot, resolveSiteDataByRoute, sanitizeFileName, slash, stackView, treatAsHtml };
import { Awaitable, escapeHtml, inBrowser, joinPath } from "../shared.js";
import { AsyncComponentLoader } from "vue";
//#region src/client/app/utils.d.ts
/**
 * Runtime base path used by the app.
 *
 * Usually this is the configured site base.
 *
 * For a relative base (`'./'`), the mount point is unknown at build time, so:
 * - SSR: uses `RELATIVE_BASE_SENTINEL` (for per-page URL relativization)
 * - dev browser: uses `'/'` (dev server always mounts at root)
 * - prod browser: resolves from the page's `__VP_SITE_ROOT__`
 */
declare function runtimeBase(): string;
/**
 * Prepend base to internal (non-relative) urls
 */
declare function withBase(path: string): string;
/**
 * Converts a url path to the corresponding js chunk filename.
 */
declare function pathToFile(path: string): string | null;
declare let contentUpdatedCallbacks: (() => any)[];
/**
 * Register callback that is called every time the markdown content is updated
 * in the DOM.
 */
declare function onContentUpdated(fn: () => any): void;
declare function defineClientComponent(loader: AsyncComponentLoader, args?: any[], cb?: () => Awaitable<void>): {
  setup(): () => import("vue").VNode<import("vue").RendererNode, import("vue").RendererElement, {
    [key: string]: any;
  }> | null;
};
//#endregion
export { escapeHtml as _escapeHtml, contentUpdatedCallbacks, defineClientComponent, inBrowser, joinPath, onContentUpdated, pathToFile, runtimeBase, withBase };
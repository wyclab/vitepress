import { Awaitable, PageData, Route } from "../shared.js";
import { Component, InjectionKey } from "vue";
//#region src/client/app/router.d.ts
interface Router {
  /**
   * Current route.
   */
  route: Route;
  /**
   * Navigate to a new URL.
   */
  go: (to: string, options?: {
    initialLoad?: boolean;
    replace?: boolean;
  }) => Promise<void>;
  /**
   * Called before the route changes. Return `false` to cancel the navigation.
   */
  onBeforeRouteChange?: (to: string) => Awaitable<void | boolean>;
  /**
   * Called before the page component is loaded (after the history state is
   * updated). Return `false` to cancel the navigation.
   */
  onBeforePageLoad?: (to: string) => Awaitable<void | boolean>;
  /**
   * Called after the page component is loaded (before the page component is updated).
   */
  onAfterPageLoad?: (to: string) => Awaitable<void>;
  /**
   * Called after the route changes.
   */
  onAfterRouteChange?: (to: string) => Awaitable<void>;
}
declare const RouterSymbol: InjectionKey<Router>;
interface PageModule {
  __pageData: PageData;
  default: Component;
}
declare function createRouter(loadPageModule: (path: string) => Awaitable<PageModule | null>, fallbackComponent?: Component): Router;
declare function useRouter(): Router;
declare function useRoute(): Route;
declare function scrollTo(hash: string, scrollPosition?: number): void;
//#endregion
export { Router, RouterSymbol, createRouter, scrollTo, useRoute, useRouter };
import { BuildOptions, ConfigEnv, Logger, Plugin, ServerOptions, UserConfig as UserConfig$1, loadEnv } from "vite";
import { URL as URL$1 } from "node:url";
import { BuiltinLanguage, BuiltinTheme, CodeToHastOptions, Highlighter, LanguageInput, ShikiTransformer, ThemeRegistrationAny } from "shiki";
import { Transform, TransformOptions } from "node:stream";
import MarkdownIt, { Options, PluginSimple, PluginWithOptions, PluginWithParams, PresetName } from "markdown-it";
import { DefaultTheme } from "../../types/default-theme.js";
import { PageSplitSection } from "../../types/local-search.js";
import { AdditionalConfig, AdditionalConfigDict, AdditionalConfigLoader, Awaitable, CodeCopyButtonOptions, ContainerOptions, HeadConfig, HeadConfig as HeadConfig$1, Header, LocaleConfig, LocaleSpecificConfig, MarkdownEnv, MarkdownLocaleOptions, MarkdownLocaleOptions as MarkdownLocaleOptions$1, PageData, SSGContext, SiteData, SiteData as SiteData$1, SiteData as SiteData$2 } from "../../types/shared.js";
import { Options as Options$1 } from "@vitejs/plugin-vue";
import { UseDarkOptions } from "@vueuse/core";
import "markdown-it/lib/parser_block.mjs";
import "markdown-it/lib/parser_inline.mjs";
import StateCore from "markdown-it/lib/rules_core/state_core.mjs";
import Token from "markdown-it/lib/token.mjs";
import { Plugin as Plugin$1 } from "postcss";
import { IncomingMessage, ServerResponse } from "http";
import { ListenOptions, Server } from "net";
//#region node_modules/.pnpm/markdown-it-async@2.2.0/node_modules/markdown-it-async/dist/index.d.mts
type PluginSimple$1 = ((md: MarkdownItAsync) => void);
type PluginWithOptions$1<T = any> = ((md: MarkdownItAsync, options?: T) => void);
type PluginWithParams$1 = ((md: MarkdownItAsync, ...params: any[]) => void);
interface MarkdownItAsyncOptions extends Omit<Options, 'highlight'> {
  /**
   * Highlighter function for fenced code blocks.
   * Highlighter `function (str, lang, attrs)` should return escaped HTML. It can
   * also return empty string if the source was not changed and should be escaped
   * externally. If result starts with <pre... internal wrapper is skipped.
   * @default null
   */
  highlight?: ((str: string, lang: string, attrs: string) => string | Promise<string>) | null | undefined;
  /**
   * Emit warning when calling `md.render` instead of `md.renderAsync`.
   *
   * @default false
   */
  warnOnSyncRender?: boolean;
}
type MarkdownItAsyncPlaceholderMap = Map<string, [promise: Promise<string>, str: string, lang: string, attrs: string]>;
declare class MarkdownItAsync extends MarkdownIt {
  // @ts-ignore
  options: MarkdownItAsyncOptions;
  placeholderMap: MarkdownItAsyncPlaceholderMap;
  private disableWarn;
  constructor(presetName: PresetName, options?: MarkdownItAsyncOptions);
  constructor(options?: MarkdownItAsyncOptions);
  use(plugin: PluginSimple$1): this;
  use(plugin: PluginSimple): this;
  use<T = any>(plugin: PluginWithOptions$1<T>, options?: T): this;
  use<T = any>(plugin: PluginWithOptions<T>, options?: T): this;
  use(plugin: PluginWithParams$1, ...params: any[]): this;
  use(plugin: PluginWithParams, ...params: any[]): this;
  render(src: string, env?: any): string;
  renderAsync(src: string, env?: any): Promise<string>;
}
//#endregion
//#region src/node/shared.d.ts
/**
 * Resolves the site data for a route, layering the matched locale and
 * additional configs over the root config.
 */
declare function resolveSiteDataByRoute(siteData: SiteData$1, relativePath: string, filePath?: string): SiteData$1;
//#endregion
//#region src/node/defaultTheme.d.ts
declare module '../../types/default-theme.js' {
  namespace DefaultTheme {
    interface LocalSearchOptions {
      /**
       * Allows transformation of content before indexing (node only)
       * Return empty string to skip indexing
       */
      _render?: (src: string, env: MarkdownEnv, md: MarkdownItAsync) => Awaitable<string>;
    }
    interface MiniSearchOptions {
      /**
       * Overrides the default regex based page splitter.
       * Supports async generator, making it possible to run in true parallel
       * (when used along with `node:child_process` or `worker_threads`)
       * ---
       * This should be especially useful for scalability reasons.
       * ---
       * @param {string} path - absolute path to the markdown source file
       * @param {string} html - document page rendered as html
       */
      _splitIntoSections?: (path: string, html: string) => AsyncGenerator<PageSplitSection> | Generator<PageSplitSection> | Awaitable<PageSplitSection[]>;
    }
  }
}
//#endregion
//#region node_modules/.pnpm/sitemap@9.0.1/node_modules/sitemap/dist/esm/lib/types.d.ts
/**
 * How frequently the page is likely to change. This value provides general
 * information to search engines and may not correlate exactly to how often they crawl the page. Please note that the
 * value of this tag is considered a hint and not a command. See
 * <https://www.sitemaps.org/protocol.html#xmlTagDefinitions> for the acceptable
 * values
 */
declare enum EnumChangefreq {
  DAILY = "daily",
  MONTHLY = "monthly",
  ALWAYS = "always",
  HOURLY = "hourly",
  WEEKLY = "weekly",
  YEARLY = "yearly",
  NEVER = "never"
}
/**
 * https://support.google.com/webmasters/answer/74288?hl=en&ref_topic=4581190
 */
interface NewsItem {
  access?: 'Registration' | 'Subscription';
  publication: {
    name: string;
    /**
     * The `<language>` is the language of your publication. Use an ISO 639
     * language code (2 or 3 letters).
     */
    language: string;
  };
  /**
   * @example 'PressRelease, Blog'
   */
  genres?: string;
  /**
   * Article publication date in W3C format, using either the "complete date" (YYYY-MM-DD) format or the "complete date
   * plus hours, minutes, and seconds"
   */
  publication_date: string;
  /**
   * The title of the news article
   * @example 'Companies A, B in Merger Talks'
   */
  title: string;
  /**
   * @example 'business, merger, acquisition'
   */
  keywords?: string;
  /**
   * @example 'NASDAQ:A, NASDAQ:B'
   */
  stock_tickers?: string;
}
/**
 * Sitemap Image
 * https://support.google.com/webmasters/answer/178636?hl=en&ref_topic=4581190
 */
interface Img {
  /**
   * The URL of the image
   * @example 'https://example.com/image.jpg'
   */
  url: string;
  /**
   * The caption of the image
   * @example 'Thanksgiving dinner'
   */
  caption?: string;
  /**
   * The title of the image
   * @example 'Star Wars EP IV'
   */
  title?: string;
  /**
   * The geographic location of the image.
   * @example 'Limerick, Ireland'
   */
  geoLocation?: string;
  /**
   * A URL to the license of the image.
   * @example 'https://example.com/license.txt'
   */
  license?: string;
}
/**
 * https://support.google.com/webmasters/answer/189077
 */
interface LinkItem {
  /**
   * @example 'en'
   */
  lang: string;
  /**
   * @example 'en-us'
   */
  hreflang?: string;
  url: string;
}
/**
 * How to handle errors in passed in urls
 */
declare enum ErrorLevel {
  /**
   * Validation will be skipped and nothing logged or thrown.
   */
  SILENT = "silent",
  /**
   * If an invalid value is encountered, a console.warn will be called with details
   */
  WARN = "warn",
  /**
   * An Error will be thrown on encountering invalid data.
   */
  THROW = "throw"
}
type ErrorHandler$1 = (error: Error, level: ErrorLevel) => void;
//#endregion
//#region node_modules/.pnpm/sitemap@9.0.1/node_modules/sitemap/dist/esm/lib/sitemap-stream.d.ts
interface NSArgs {
  news: boolean;
  video: boolean;
  xhtml: boolean;
  image: boolean;
  custom?: string[];
}
interface SitemapStreamOptions extends TransformOptions {
  hostname?: string;
  level?: ErrorLevel;
  lastmodDateOnly?: boolean;
  xmlns?: NSArgs;
  xslUrl?: string;
  errorHandler?: ErrorHandler$1;
}
//#endregion
//#region src/node/plugin.d.ts
declare module 'vite' {
  interface UserConfig {
    vitepress?: SiteConfig;
  }
}
//#endregion
//#region src/node/build/generateSitemap.d.ts
interface SitemapItem {
  lastmod?: string | number | Date;
  changefreq?: `${EnumChangefreq}`;
  fullPrecisionPriority?: boolean;
  priority?: number;
  news?: NewsItem;
  expires?: string;
  androidLink?: string;
  ampLink?: string;
  url: string;
  video?: any;
  img?: string | Img | (string | Img)[];
  links?: LinkItem[];
  lastmodfile?: string | Buffer | URL;
  lastmodISO?: string;
  lastmodrealtime?: boolean;
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+plugin-component@3.0.2/node_modules/@mdit-vue/plugin-component/dist/index.d.ts
//#region src/types.d.ts
/**
 * Options of @mdit-vue/plugin-component
 */
interface ComponentPluginOptions {
  /**
   * Extra tags to be treated as block tags.
   *
   * @default []
   */
  blockTags?: string[];
  /**
   * Extra tags to be treated as inline tags.
   *
   * @default []
   */
  inlineTags?: string[];
}
//#endregion
//#region node_modules/.pnpm/gray-matter@4.0.3/node_modules/gray-matter/gray-matter.d.ts
/**
 * Takes a string or object with `content` property, extracts
 * and parses front-matter from the string, then returns an object
 * with `data`, `content` and other [useful properties](#returned-object).
 *
 * ```js
 * var matter = require('gray-matter');
 * console.log(matter('---\ntitle: Home\n---\nOther stuff'));
 * //=> { data: { title: 'Home'}, content: 'Other stuff' }
 * ```
 * @param {Object|String} `input` String, or object with `content` string
 * @param {Object} `options`
 * @return {Object}
 * @api public
 */
declare function matter<I extends matter.Input, O extends matter.GrayMatterOption<I, O>>(input: I | {
  content: I;
}, options?: O): matter.GrayMatterFile<I>;
declare namespace matter {
  type Input = string | Buffer;
  interface GrayMatterOption<I extends Input, O extends GrayMatterOption<I, O>> {
    parser?: () => void;
    eval?: boolean;
    excerpt?: boolean | ((input: I, options: O) => string);
    excerpt_separator?: string;
    engines?: {
      [index: string]: ((input: string) => object) | {
        parse: (input: string) => object;
        stringify?: (data: object) => string;
      };
    };
    language?: string;
    delimiters?: string | [string, string];
  }
  interface GrayMatterFile<I extends Input> {
    data: {
      [key: string]: any;
    };
    content: string;
    excerpt?: string;
    orig: Buffer | I;
    language: string;
    matter: string;
    stringify(lang: string): string;
  }
  /**
   * Stringify an object to YAML or the specified language, and
   * append it to the given string. By default, only YAML and JSON
   * can be stringified. See the [engines](#engines) section to learn
   * how to stringify other languages.
   *
   * ```js
   * console.log(matter.stringify('foo bar baz', {title: 'Home'}));
   * // results in:
   * // ---
   * // title: Home
   * // ---
   * // foo bar baz
   * ```
   * @param {String|Object} `file` The content string to append to stringified front-matter, or a file object with `file.content` string.
   * @param {Object} `data` Front matter to stringify.
   * @param {Object} `options` [Options](#options) to pass to gray-matter and [js-yaml].
   * @return {String} Returns a string created by wrapping stringified yaml with delimiters, and appending that to the given string.
   */
  export function stringify<O extends GrayMatterOption<string, O>>(file: string | {
    content: string;
  }, data: object, options?: GrayMatterOption<string, O>): string;
  /**
   * Synchronously read a file from the file system and parse
   * front matter. Returns the same object as the [main function](#matter).
   *
   * ```js
   * var file = matter.read('./content/blog-post.md');
   * ```
   * @param {String} `filepath` file path of the file to read.
   * @param {Object} `options` [Options](#options) to pass to gray-matter.
   * @return {Object} Returns [an object](#returned-object) with `data` and `content`
   */
  export function read<O extends GrayMatterOption<string, O>>(fp: string, options?: GrayMatterOption<string, O>): matter.GrayMatterFile<string>;
  /**
   * Returns true if the given `string` has front matter.
   * @param  {String} `string`
   * @param  {Object} `options`
   * @return {Boolean} True if front matter exists.
   */
  export function test<O extends matter.GrayMatterOption<string, O>>(str: string, options?: GrayMatterOption<string, O>): boolean;
  /**
   * Detect the language to use, if one is defined after the
   * first front-matter delimiter.
   * @param  {String} `string`
   * @param  {Object} `options`
   * @return {Object} Object with `raw` (actual language string), and `name`, the language with whitespace trimmed
   */
  export function language<O extends matter.GrayMatterOption<string, O>>(str: string, options?: GrayMatterOption<string, O>): {
    name: string;
    raw: string;
  };
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+plugin-frontmatter@3.0.2/node_modules/@mdit-vue/plugin-frontmatter/dist/index.d.ts
//#region src/types.d.ts
type GrayMatterOptions = matter.GrayMatterOption<string, GrayMatterOptions>;
/**
 * Options of @mdit-vue/plugin-frontmatter
 */
interface FrontmatterPluginOptions {
  /**
   * Options of gray-matter
   *
   * @see https://github.com/jonschlinkert/gray-matter#options
   */
  grayMatterOptions?: GrayMatterOptions;
  /**
   * Render the excerpt or not
   *
   * @default true
   */
  renderExcerpt?: boolean;
}
declare module '@mdit-vue/types' {
  interface MarkdownItEnv {
    /**
     * The raw Markdown content without frontmatter
     */
    content?: string;
    /**
     * The excerpt that extracted by `@mdit-vue/plugin-frontmatter`
     *
     * - Would be the rendered HTML when `renderExcerpt` is enabled
     * - Would be the raw Markdown when `renderExcerpt` is disabled
     */
    excerpt?: string;
    /**
     * The frontmatter that extracted by `@mdit-vue/plugin-frontmatter`
     */
    frontmatter?: Record<string, unknown>;
  }
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+types@3.0.2/node_modules/@mdit-vue/types/dist/index.d.ts
interface MarkdownItHeader {
  /**
   * The level of the header
   *
   * `1` to `6` for `<h1>` to `<h6>`
   */
  level: number;
  /**
   * The title of the header
   */
  title: string;
  /**
   * The slug of the header
   *
   * Typically the `id` attr of the header anchor
   */
  slug: string;
  /**
   * Link of the header
   *
   * Typically using `#${slug}` as the anchor hash
   */
  link: string;
  /**
   * The children of the header
   */
  children: MarkdownItHeader[];
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+plugin-headers@3.0.2/node_modules/@mdit-vue/plugin-headers/dist/index.d.ts
//#region src/types.d.ts
/**
 * Options of @mdit-vue/plugin-headers
 */
interface HeadersPluginOptions {
  /**
   * A custom slugification function
   *
   * Should use the same slugify function with markdown-it-anchor
   * to ensure the link is matched
   */
  slugify?: (str: string) => string;
  /**
   * A function for formatting header title
   */
  format?: (str: string) => string;
  /**
   * Heading level that going to be extracted
   *
   * Should be a subset of markdown-it-anchor's `level` option
   * to ensure the slug is existed
   *
   * @default [2,3]
   */
  level?: number[];
  /**
   * Should allow headers inside nested blocks or not
   *
   * If set to `true`, headers inside blockquote, list, etc. would also be extracted.
   *
   * @default false
   */
  shouldAllowNested?: boolean;
}
declare module '@mdit-vue/types' {
  interface MarkdownItEnv {
    /**
     * The headers that extracted by `@mdit-vue/plugin-headers`
     */
    headers?: MarkdownItHeader[];
  }
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+plugin-sfc@3.0.2/node_modules/@mdit-vue/plugin-sfc/dist/index.d.ts
//#endregion
//#region src/types.d.ts
/**
 * Options of @mdit-vue/plugin-sfc
 */
interface SfcPluginOptions {
  /**
   * Custom blocks to be extracted
   *
   * @default []
   */
  customBlocks?: string[];
}
/**
 * SFC block that extracted from markdown
 */
interface SfcBlock {
  /**
   * The type of the block
   */
  type: string;
  /**
   * The content, including open-tag and close-tag
   */
  content: string;
  /**
   * The content that stripped open-tag and close-tag off
   */
  contentStripped: string;
  /**
   * The open-tag
   */
  tagOpen: string;
  /**
   * The close-tag
   */
  tagClose: string;
}
interface MarkdownSfcBlocks {
  /**
   * The `<template>` block
   */
  template: SfcBlock | null;
  /**
   * The common `<script>` block
   */
  script: SfcBlock | null;
  /**
   * The `<script setup>` block
   */
  scriptSetup: SfcBlock | null;
  /**
   * All `<script>` blocks.
   *
   * By default, SFC only allows one `<script>` block and one `<script setup>` block.
   * However, some tools may support different types of `<script>`s, so we keep all of them here.
   */
  scripts: SfcBlock[];
  /**
   * All `<style>` blocks.
   */
  styles: SfcBlock[];
  /**
   * All custom blocks.
   */
  customBlocks: SfcBlock[];
}
declare module '@mdit-vue/types' {
  interface MarkdownItEnv {
    /**
     * SFC blocks that extracted by `@mdit-vue/plugin-sfc`
     */
    sfcBlocks?: MarkdownSfcBlocks;
  }
}
//#endregion
//#region node_modules/.pnpm/@mdit-vue+plugin-toc@3.0.2/node_modules/@mdit-vue/plugin-toc/dist/index.d.ts
//#region src/types.d.ts
/**
 * Options of @mdit-vue/plugin-toc
 */
interface TocPluginOptions {
  /**
   * The pattern serving as the TOC placeholder in your markdown
   *
   * @default /^\[\[toc\]\]$/i
   */
  pattern?: RegExp;
  /**
   * A custom slugification function
   *
   * Should use the same slugify function with markdown-it-anchor
   * to ensure the link is matched
   */
  slugify?: (str: string) => string;
  /**
   * A function for formatting headings
   */
  format?: (str: string) => string;
  /**
   * Heading level that going to be included in the TOC
   *
   * Should be a subset of markdown-it-anchor's `level` option
   * to ensure the link is existed
   *
   * @default [2,3]
   */
  level?: number[];
  /**
   * Should allow headers inside nested blocks or not
   *
   * If set to `true`, headers inside blockquote, list, etc. would also be included.
   *
   * @default false
   */
  shouldAllowNested?: boolean;
  /**
   * HTML tag of the TOC container
   *
   * @default 'nav'
   */
  containerTag?: string;
  /**
   * The class for the TOC container
   *
   * @default 'table-of-contents'
   */
  containerClass?: string;
  /**
   * HTML tag of the TOC list
   *
   * @default 'ul'
   */
  listTag?: 'ol' | 'ul';
  /**
   * The class for the TOC list
   *
   * @default ''
   */
  listClass?: string;
  /**
   * The class for the `<li>` tag
   *
   * @default ''
   */
  itemClass?: string;
  /**
   * The tag of the link inside `<li>` tag
   *
   * @default 'a'
   */
  linkTag?: 'a' | 'router-link';
  /**
   * The class for the link inside the `<li>` tag
   *
   * @default ''
   */
  linkClass?: string;
}
//#endregion
//#region node_modules/.pnpm/@mdit+plugin-anchor@1.1.3_markdown-it@14.3.0/node_modules/@mdit/plugin-anchor/dist/index.d.ts
/**
 * Permalink generator function
 *
 * 永久链接生成器函数
 */
type PermalinkGenerator = (slug: string, options: ResolvedAnchorOptions, state: StateCore, index: number) => void;
//#endregion
//#region src/options.d.ts
/**
 * Information about a heading anchor
 *
 * 标题锚点信息
 */
interface AnchorInfo {
  /**
   * Generated slug
   *
   * 生成的 slug
   */
  slug: string;
  /**
   * Original heading title
   *
   * 原始标题文本
   */
  title: string;
}
/**
 * Options for the anchor plugin
 *
 * 锚点插件选项
 */
interface AnchorOptions {
  /**
   * Heading levels to add anchors to (array or minimum level)
   *
   * 要添加锚点的标题级别（数组或最低级别）
   *
   * @default 1
   */
  level?: number | number[];
  /**
   * Function to generate slugs from heading text
   *
   * 从标题文本生成 slug 的函数
   */
  slugify?: (str: string) => string;
  /**
   * Function to generate slugs with access to state
   *
   * 可访问 state 的生成 slug 函数
   */
  slugifyWithState?: (str: string, state: StateCore) => string;
  /**
   * Function to extract text from tokens
   *
   * 从 token 中提取文本的函数
   */
  getTokensText?: (tokens: Token[]) => string;
  /**
   * Starting index for unique slug numbering
   *
   * 唯一 slug 编号的起始索引
   *
   * @default 1
   */
  uniqueSlugStartIndex?: number;
  /**
   * Permalink generator function
   *
   * 永久链接生成器函数
   */
  permalink?: PermalinkGenerator;
  /**
   * Callback invoked for each heading with anchor info
   *
   * 每个标题添加锚点后调用的回调
   */
  callback?: (token: Token, anchorInfo: AnchorInfo) => void;
  /**
   * Value of the `tabindex` attribute on headings, set to `false` to disable
   *
   * 标题上 `tabindex` 属性的值，设为 `false` 禁用
   *
   * @default "-1"
   */
  tabIndex?: string | number | false;
}
/**
 * Anchor options with defaults applied, as received by permalink generators
 *
 * 应用默认值后的锚点选项，即永久链接生成器接收到的选项
 */
type ResolvedAnchorOptions = AnchorOptions & Required<Pick<AnchorOptions, "getTokensText" | "level" | "slugify" | "tabIndex" | "uniqueSlugStartIndex">>;
//#endregion
//#region node_modules/.pnpm/@mdit+plugin-attrs@1.1.1_markdown-it@14.3.0/node_modules/@mdit/plugin-attrs/dist/index.d.ts
//#region src/helper/types.d.ts
interface AllowedAttrEntry {
  /**
   * Attribute name, supports string or RegExp
   *
   * 属性名，支持字符串或正则
   */
  name: string | RegExp;
  /**
   * Allowed attribute values, supports string or RegExp array
   *
   * Empty or omitted means allowing any value
   *
   * 允许的属性值，支持字符串或正则数组
   *
   * 空数组或省略表示允许任意值
   */
  value?: (string | RegExp)[];
}
type AllowedAttrs = (string | RegExp)[] | AllowedAttrEntry[];
/**
 * Normalized attribute filter for fast matching
 *
 * 正则化后的属性过滤器，用于快速匹配
 */
type AttrFilter = (name: string, value: string) => boolean;
interface DelimiterConfig {
  /**
   * Left delimiter
   *
   * 左分隔符
   *
   * @default "{"
   */
  left: string;
  /**
   * Right delimiter
   *
   * 右分隔符
   *
   * @default "}"
   */
  right: string;
  /**
   * Allowed attributes (raw config)
   *
   * 允许的属性（原始配置）
   *
   * @default [ ]
   */
  allowed: AllowedAttrs;
  /**
   * Normalized attribute filter
   *
   * 正则化后的属性过滤器
   */
  filter: AttrFilter | null;
}
//#endregion
//#region src/options.d.ts
type MarkdownItAttrRuleName = "fence" | "inline" | "table" | "list" | "heading" | "hr" | "softbreak" | "blockInfo" | "blockEnd" |
/** Legacy alias of `blockEnd` */
"block" |
/** Opt-in task list label support - excluded from `"all"` */
"tasklist" |
/** Opt-in definition list support - excluded from `"all"` */
"dl";
interface MarkdownItAttrsOptions extends Partial<DelimiterConfig> {
  /**
   * Rules to enable
   *
   * 启用的规则
   *
   * @default "all"
   */
  rule?: "all" | boolean | MarkdownItAttrRuleName[];
  /**
   * Place fence attributes on `<pre>` instead of `<code>`
   *
   * 将代码块属性放在 `<pre>` 上而非 `<code>` 上
   *
   * @default true
   */
  fenceAttrsOnPre?: boolean;
}
//#endregion
//#region node_modules/.pnpm/@mdit+plugin-emoji@1.1.1_markdown-it@14.3.0/node_modules/@mdit/plugin-emoji/dist/index.d.ts
//#region src/options.d.ts
interface EmojiPluginOptions$1 {
  /**
   * Emoji definitions
   *
   * 表情定义
   *
   * The key is the emoji name, and the value is the URL of the emoji character. 键是表情名称，值是表情字符。
   */
  definitions?: Record<string, string>;
  /**
   * Enabled emojis
   *
   * 启用的表情
   *
   * If specified, only emojis in this list will be rendered. Otherwise, all emojis in the
   * definitions will be rendered. 如果指定了这个选项，只有在这个列表中的表情才会被渲染。否则，定义中的所有表情都会被渲染。
   *
   * @default [ ]
   */
  enabled?: string[];
  /**
   * Shortcut definitions
   *
   * 快捷键定义
   *
   * The key is the emoji name, and the value is the shortcut(s) for the emoji. 键是表情名称，值是表情的快捷短语。
   *
   * @default {}
   */
  shortcuts?: Record<string, string | string[]>;
}
//#endregion
//#region src/full.d.ts
declare const fullEmoji: PluginWithOptions<EmojiPluginOptions$1>;
//#endregion
//#region node_modules/.pnpm/@mdit+plugin-tasklist@1.0.2_markdown-it@14.3.0/node_modules/@mdit/plugin-tasklist/dist/index.d.ts
//#region src/options.d.ts
interface MarkdownItTaskListOptions {
  /**
   * Whether disable checkbox
   *
   * 是否禁用 checkbox
   *
   * @default true
   */
  disabled?: boolean;
  /**
   * Whether use `<label>` to wrap text
   *
   * 是否使用 `<label>` 来包裹文字
   *
   * @default true
   */
  label?: boolean;
  /**
   * Class for tasklist container
   *
   * Tasklist 容器的 class
   *
   * @default "task-list-container"
   */
  containerClass?: string;
  /**
   * Class for tasklist item
   *
   * Tasklist item 的 class
   *
   * @default "task-list-item"
   */
  itemClass?: string;
  /**
   * Class for tasklist item label
   *
   * Tasklist item label 的 class
   *
   * @default "task-list-item-label"
   */
  labelClass?: string;
  /**
   * Class for tasklist item checkbox
   *
   * Tasklist item checkbox 的 class
   *
   * @default "task-list-item-checkbox"
   */
  checkboxClass?: string;
}
//#endregion
//#region src/node/markdown/plugins/image.d.ts
interface Options$5 {
  /**
   * Support native lazy loading for the `<img>` tag.
   * @default false
   */
  lazyLoad?: boolean;
}
//#endregion
//#region src/node/markdown/plugins/include.d.ts
interface Options$4 {
  /**
   * Log a warning and expand to nothing when the included file, region,
   * heading or range is missing, instead of throwing.
   * @default false
   */
  silent?: boolean;
  /**
   * Rewrite relative image and link urls inside included markdown files so
   * they resolve from the included file's location instead of the page
   * including it.
   * @default true
   */
  rebaseRelativeUrls?: boolean;
}
//#endregion
//#region src/node/markdown/plugins/snippet.d.ts
interface Options$3 {
  /**
   * Log a warning and render nothing when the snippet file or region is
   * missing, instead of throwing.
   * @default false
   */
  silent?: boolean;
  /**
   * Which region marker lines to remove from snippet output: `true` removes
   * only markers of the style(s) that matched the requested region, so
   * whole-file imports keep theirs, `'all'` removes markers of every style
   * and name, and `false` keeps all of them.
   * @default true
   */
  stripRegionMarkers?: boolean | 'all';
}
//#endregion
//#region src/node/markdown/markdown.d.ts
type EmojiPluginOptions = NonNullable<Parameters<typeof fullEmoji>[1]>;
type ThemeOptions = ThemeRegistrationAny | BuiltinTheme | {
  light: ThemeRegistrationAny | BuiltinTheme;
  dark: ThemeRegistrationAny | BuiltinTheme;
};
type MarkdownRenderer = MarkdownItAsync & {
  options: {
    highlight?: any;
  };
};
interface MarkdownOptions extends MarkdownItAsyncOptions {
  /**
   * Configure the markdown-it instance before any plugins are applied.
   */
  preConfig?: (md: MarkdownRenderer) => Awaitable<void>;
  /**
   * Configure the markdown-it instance after all built-in plugins are applied.
   */
  config?: (md: MarkdownRenderer) => Awaitable<void>;
  /**
   * Disable cache (experimental)
   */
  cache?: boolean;
  /**
   * HTML attributes applied to external links.
   * @default { target: '_blank', rel: 'noreferrer' }
   */
  externalLinks?: Record<string, string>;
  /**
   * Per-locale overrides for build-time markdown strings (container titles
   * and the code copy button title), keyed by locale index. Populated
   * automatically from `locales.<index>.markdown` in the site config - pass
   * directly only when using `createMarkdownRenderer` standalone.
   */
  locales?: Record<string, MarkdownLocaleOptions$1>;
  /**
   * Custom theme for syntax highlighting.
   *
   * You can also pass an object with `light` and `dark` themes to support
   * dual themes.
   *
   * @example { theme: 'github-dark' }
   * @example { theme: { light: 'github-light', dark: 'github-dark' } }
   *
   * You can use an existing theme.
   * @see https://shiki.style/themes
   * Or add your own theme.
   * @see https://shiki.style/guide/load-theme
   */
  theme?: ThemeOptions;
  /**
   * Custom languages for syntax highlighting or pre-load built-in languages.
   * @see https://shiki.style/languages
   */
  languages?: (LanguageInput | BuiltinLanguage)[];
  /**
   * Custom language aliases for syntax highlighting.
   * Maps custom language names to existing languages.
   * Alias lookup is case-insensitive and underscores in language names are
   * displayed as spaces.
   *
   * @example
   *
   * Maps `my_lang` to use Python syntax highlighting.
   * ```js
   * { 'my_lang': 'python' }
   * ```
   *
   * Usage in markdown:
   * ````md
   * ```My_Lang
   * # This will be highlighted as Python code
   * # and will show "My Lang" as the language label
   * print("Hello, World!")
   * ```
   * ````
   *
   * @see https://shiki.style/guide/load-lang#custom-language-aliases
   */
  languageAlias?: Record<string, string>;
  /**
   * Fallback language used when the specified language is not available.
   */
  defaultHighlightLang?: string;
  /**
   * Transformers applied to code blocks.
   * @see https://shiki.style/guide/transformers
   */
  codeTransformers?: ShikiTransformer[];
  /**
   * Color replacements applied during syntax highlighting.
   * Accepts either a flat color map or per-theme replacements.
   * @see https://shiki.style/guide/theme-colors#color-replacements
   */
  colorReplacements?: CodeToHastOptions['colorReplacements'];
  /**
   * Configure the Shiki instance.
   */
  shikiSetup?: (shiki: Highlighter) => void | Promise<void>;
  /**
   * Wrap code blocks in a container carrying the language label and the
   * copy button. The default theme's code block styling relies on this
   * markup. Disabling it also disables `lineNumbers`.
   * @default true
   */
  preWrapper?: boolean;
  /**
   * Strings for the copy button in code blocks: `tooltipText` is the
   * button's tooltip and `copiedText` is shown next to it after copying.
   * @default { tooltipText: 'Copy code', copiedText: 'Copied' }
   */
  codeCopyButton?: CodeCopyButtonOptions;
  /**
   * Custom language labels for display.
   * Overrides the default language label shown in code blocks.
   * Keys are case-insensitive.
   *
   * @example { 'vue': 'Vue SFC' }
   */
  languageLabel?: Record<string, string>;
  /**
   * Show line numbers in code blocks. Requires the `preWrapper` plugin.
   * @default false
   */
  lineNumbers?: boolean;
  /**
   * Options for importing code snippets from files with `<<<`. Set to
   * `false` to disable.
   * @see https://vitepress.dev/guide/markdown#import-code-snippets
   */
  snippet?: Options$3 | boolean;
  /**
   * Options for including markdown files with `<!-- @include: path -->`.
   * Set to `false` to disable.
   * @see https://vitepress.dev/guide/markdown#markdown-file-inclusion
   */
  include?: Options$4 | boolean;
  /**
   * Options for `@mdit/plugin-attrs`. Set to `false` to disable. The `fence`
   * rule is off by default so that curly attributes never consume code block
   * meta (e.g. line highlighting) - add classes to code blocks using shiki
   * transformers instead.
   * @see https://mdit-plugins.github.io/attrs.html
   */
  attrs?: MarkdownItAttrsOptions | boolean;
  /**
   * Options for `@mdit/plugin-emoji`. Set to `false` to disable.
   * @see https://mdit-plugins.github.io/emoji.html
   */
  emoji?: EmojiPluginOptions | boolean;
  /**
   * Options for `@mdit/plugin-tasklist` (GitHub-style task lists,
   * `- [ ] task`). Set to `false` to disable.
   * @see https://mdit-plugins.github.io/tasklist.html
   */
  tasklist?: MarkdownItTaskListOptions | boolean;
  /**
   * Whether to enable footnotes (`[^1]` references with definitions, plus
   * inline `^[note]` syntax).
   * @default true
   * @see https://mdit-plugins.github.io/footnote.html
   */
  footnote?: boolean;
  /**
   * Improves emphasis (`**bold**`) handling in Japanese, Chinese, and
   * Korean text.
   * @default true
   * @see https://github.com/tats-u/markdown-cjk-friendly
   */
  cjkFriendlyEmphasis?: boolean;
  /**
   * Options for `@mdit/plugin-anchor`. Set to `false` to disable adding ids
   * and anchor links to headings. Note that the default theme's outline and
   * heading hash links rely on these ids.
   * @see https://mdit-plugins.github.io/anchor.html
   */
  anchor?: AnchorOptions | boolean;
  /**
   * Options for `@mdit-vue/plugin-headers`. Set to `true` or pass options
   * to collect page headers into page data.
   * @default false
   * @see https://github.com/mdit-vue/mdit-vue/tree/main/packages/plugin-headers
   */
  headers?: HeadersPluginOptions | boolean;
  /**
   * Options for `@mdit-vue/plugin-toc`. Set to `false` to disable the
   * `[[toc]]` syntax.
   * @see https://github.com/mdit-vue/mdit-vue/tree/main/packages/plugin-toc
   */
  toc?: TocPluginOptions | boolean;
  /**
   * Math support.
   *
   * You need to install `markdown-it-mathjax3` and set `math` to `true` to
   * enable it. You can also pass options to `markdown-it-mathjax3` here.
   * @default false
   * @see https://vitepress.dev/guide/markdown#math-equations
   */
  math?: any | boolean;
  /**
   * Custom labels for the built-in containers (`::: tip` etc.) and
   * additional user-defined containers. Labels are also used as the
   * default titles of GitHub-flavored alerts.
   * @see https://vitepress.dev/guide/markdown#custom-containers
   */
  container?: ContainerOptions | boolean;
  /**
   * Whether to enable GitHub-flavored alerts (`> [!NOTE]`).
   * @default true
   * @see https://vitepress.dev/guide/markdown#github-flavored-alerts
   */
  gfmAlerts?: boolean;
  /**
   * Add `tabindex="0"` to tables so keyboard users can focus and scroll
   * them.
   * @default true
   */
  tableTabIndex?: boolean;
  /**
   * Options for the image plugin (resolves image sources against the public
   * directory, adds dimensions, and supports lazy loading). Set to `false`
   * to disable.
   * @see https://vitepress.dev/guide/markdown#image-lazy-loading
   */
  image?: Options$5 | boolean;
  /**
   * Options for `@mdit-vue/plugin-component`. Set to `false` to disable.
   * @see https://github.com/mdit-vue/mdit-vue/tree/main/packages/plugin-component
   */
  component?: ComponentPluginOptions | boolean;
  /**
   * Options for `@mdit-vue/plugin-frontmatter`.
   * @see https://github.com/mdit-vue/mdit-vue/tree/main/packages/plugin-frontmatter
   */
  frontmatter?: FrontmatterPluginOptions;
  /**
   * Resolve `{{ $frontmatter.<path> }}` interpolations to their values while
   * rendering markdown, so the value also reaches consumers that never run
   * Vue - heading anchors and titles, the local search index, content loader
   * output, link destinations - and the compiled Vue template gets static
   * text instead of a runtime expression. Only bare property paths resolving
   * to simple primitive values in the page's own frontmatter are inlined -
   * anything else (complex expressions, missing keys, non-primitive values,
   * `v-pre` scopes) keeps its runtime interpolation. Set to `false` to leave
   * all interpolation to the Vue runtime - for example when
   * `transformPageData` rewrites frontmatter values that pages interpolate,
   * which would otherwise render the pre-transform value (a warning is
   * logged when that happens).
   *
   * @experimental
   * @default true
   */
  eagerFrontmatterInterpolation?: boolean;
  /**
   * Options for `@mdit-vue/plugin-sfc`.
   * @see https://github.com/mdit-vue/mdit-vue/tree/main/packages/plugin-sfc
   */
  sfc?: SfcPluginOptions;
}
declare function mergeMarkdownLocales(options?: MarkdownOptions, locales?: LocaleConfig): MarkdownOptions;
declare function disposeMdItInstance(): void;
/**
 * @experimental
 */
declare function createMarkdownRenderer(srcDir: string, options?: MarkdownOptions, base?: string, logger?: Pick<Logger, 'warn'>, publicDir?: string): Promise<MarkdownRenderer>;
//#endregion
//#region src/node/utils/glob.d.ts
interface GlobOptions {
  absolute?: boolean;
  cwd?: string;
  ignore?: string | string[];
  dot?: boolean;
  debug?: boolean;
}
//#endregion
//#region src/node/plugins/dynamicRoutesPlugin.d.ts
interface UserRouteConfig {
  params: Record<string, string>;
  content?: string;
}
type ResolvedRouteConfig = UserRouteConfig & {
  /**
   * the raw route (relative to src root), e.g. foo/[bar].md
   */
  route: string;
  /**
   * the actual path with params resolved (relative to src root), e.g. foo/1.md
   */
  path: string;
  /**
   * absolute fs path
   */
  fullPath: string;
  /**
   * the path to the paths loader module
   */
  loaderPath: string;
};
interface RouteModule {
  watch?: string[] | string;
  paths: UserRouteConfig[] | ((watchedFiles: string[]) => Awaitable<UserRouteConfig[]>);
  transformPageData?: UserConfig['transformPageData'];
  options?: {
    globOptions?: GlobOptions;
  };
}
/**
 * Helper for defining routes with type inference
 */
declare function defineRoutes(loader: RouteModule): RouteModule;
type Optional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
declare function resolvePages(siteConfig: Optional<SiteConfig, 'pages' | 'dynamicRoutes' | 'rewrites'>, rebuildCache?: boolean): Promise<void>;
//#endregion
//#region src/node/siteConfig.d.ts
/**
 * A config object, or a function returning one. Both can be async.
 */
type RawConfigExports<ThemeConfig = any> = Awaitable<UserConfig<ThemeConfig>> | (() => Awaitable<UserConfig<ThemeConfig>>);
/**
 * Context passed to the `transformHead` and `transformHtml` build hooks.
 */
interface TransformContext<ThemeConfig = any> {
  /**
   * Name of the output HTML file, relative to the output directory.
   */
  page: string;
  /**
   * The resolved site config.
   */
  siteConfig: SiteConfig<ThemeConfig>;
  /**
   * The resolved site data.
   */
  siteData: SiteData$2;
  /**
   * Data of the page being rendered.
   */
  pageData: PageData;
  /**
   * Full title of the page, including the site title suffix.
   */
  title: string;
  /**
   * Description of the page.
   */
  description: string;
  /**
   * Head entries that are going to be written to the page.
   */
  head: HeadConfig$1[];
  /**
   * Rendered HTML of the page content.
   */
  content: string;
  /**
   * Assets referenced by the page.
   */
  assets: string[];
}
/**
 * Context passed to the `transformPageData` hook.
 */
interface TransformPageContext<ThemeConfig = any> {
  /**
   * The resolved site config.
   */
  siteConfig: SiteConfig<ThemeConfig>;
}
/**
 * VitePress config, usually defined in `.vitepress/config.[ext]`.
 */
interface UserConfig<ThemeConfig = any> extends LocaleSpecificConfig<ThemeConfig> {
  /**
   * Config to inherit from. Its values are recursively merged with (and
   * overridden by) this config. Commonly used to extend a base config
   * shared by a theme.
   */
  extends?: RawConfigExports<ThemeConfig>;
  /**
   * The base URL the site is deployed at. Usually starts and ends with a
   * slash. Use `'./'` to make page references relative to their own depth,
   * so the output works at any subpath.
   * @default '/'
   */
  base?: string;
  /**
   * Directory containing the markdown source files, relative to the
   * project root.
   * @default '.'
   */
  srcDir?: string;
  /**
   * Glob patterns for source files to exclude from the site.
   * @example ['**\/README.md', '**\/TODO.md']
   */
  srcExclude?: string[];
  /**
   * Build output location, relative to the project root.
   * @default './.vitepress/dist'
   */
  outDir?: string;
  /**
   * Directory for assets within the build output. Must not be outside
   * of `outDir`.
   * @default 'assets'
   */
  assetsDir?: string;
  /**
   * URL prefix for built assets (everything under `assetsDir`), e.g. a CDN.
   *
   * Must be one of:
   * - an absolute URL
   * - a protocol-relative URL
   * - a root-absolute path
   *
   * The prefix must mirror `outDir` layout: each asset URL = this prefix +
   * file output-relative path.
   *
   * These still use `base`:
   * - pages
   * - `withBase` links
   * - `public/` files
   * - `hashmap.json`
   *
   * If the prefix is cross-origin, it must serve CORS headers, because
   * generated tags are marked `crossorigin`.
   *
   * Applies to builds and preview (not dev).
   *
   * @example 'https://cdn.example.com/'
   */
  assetsBase?: string;
  /**
   * Options for the generated icon stylesheet (`vp-icons.*.css`).
   */
  icons?: {
    /**
     * Fully qualified `collection:name` icons to include in addition to
     * the ones collected during SSR — for icons that only render
     * client-side (e.g. inside `<ClientOnly>`).
     * @example ['mdi:home', 'simple-icons:discord']
     */
    include?: string[];
  };
  /**
   * Directory for cache files, relative to the project root.
   * @default './.vitepress/cache'
   */
  cacheDir?: string;
  /**
   * Decides whether a page emits a preload link for an async chunk.
   * All chunks are preloaded by default.
   */
  shouldPreload?: (link: string, page: string) => boolean;
  /**
   * Locale-specific configs, keyed by the locale's path prefix (with
   * `root` denoting the default locale).
   * @see https://vitepress.dev/guide/i18n
   */
  locales?: LocaleConfig<ThemeConfig>;
  /**
   * Client router options.
   */
  router?: {
    /**
     * Prefetch the chunks of in-viewport links in idle time.
     * @default true
     */
    prefetchLinks?: boolean;
  };
  /**
   * Dark mode handling:
   * - `true`: light by default, with a user toggle
   * - `'dark'`: dark by default, with a user toggle
   * - `false`: no dark mode
   * - `'force-dark'`: always dark
   * - `'force-auto'`: always follow the system preference
   * - options for `@vueuse/core`'s `useDark`
   * @default true
   */
  appearance?: boolean | 'dark' | 'force-dark' | 'force-auto' | (Omit<UseDarkOptions, 'initialValue'> & {
    initialValue?: 'dark';
  });
  /**
   * Show the timestamp of each page's last git commit.
   * @default false
   */
  lastUpdated?: boolean;
  /**
   * Custom props passed to the `<Content />` component.
   */
  contentProps?: Record<string, any>;
  /**
   * Markdown rendering options.
   */
  markdown?: MarkdownOptions;
  /**
   * Options passed to `@vitejs/plugin-vue`.
   */
  vue?: Options$1;
  /**
   * Vite config to merge with the default one. `configFile` can point
   * to an additional vite config file to load, or be `false` to load
   * none.
   */
  vite?: UserConfig$1 & {
    configFile?: string | false;
  };
  /**
   * Enable MPA / zero-JS mode: pages ship without a client-side
   * JavaScript payload and navigation does full page loads.
   * @experimental
   * @default false
   * @see https://vitepress.dev/guide/mpa-mode
   */
  mpa?: boolean;
  /**
   * Don't fail builds due to dead links. Accepts `true` (ignore all),
   * `'localhostLinks'` (only ignore localhost links), or an array of
   * exact strings, regexes, and custom filter functions.
   * @default false
   */
  ignoreDeadLinks?: boolean | 'localhostLinks' | (string | RegExp | ((link: string, source: string) => boolean))[];
  /**
   * Generate `/foo` instead of `/foo.html` for pages and internal
   * links. Requires matching support from the hosting platform.
   * @default false
   * @see https://vitepress.dev/guide/routing#generating-clean-url
   */
  cleanUrls?: boolean;
  /**
   * Use web fonts instead of emitting font files to dist. Requires the
   * active theme to import a file named `fonts.(s)css`, with its web font
   * imports placed between `webfont-marker-begin` and `webfont-marker-end`
   * comments.
   * @experimental
   * @default true in webcontainers, else false
   */
  useWebFonts?: boolean;
  /**
   * Number of pages rendered concurrently during the build. Lower
   * values reduce the memory usage at the cost of build time.
   * @default 64
   */
  buildConcurrency?: number;
  /**
   * Source-to-destination page path mappings, or a function returning
   * the destination path for a source path. Used to serve pages at
   * URLs different from their directory structure.
   * @see https://vitepress.dev/guide/routing#route-rewrites
   */
  rewrites?: Record<string, string> | ((id: string) => string);
  /**
   * Generate a `sitemap.xml` during build. `transformItems` can adjust
   * the entries before they are written.
   * @see https://vitepress.dev/guide/sitemap-generation
   */
  sitemap?: SitemapStreamOptions & {
    hostname: string;
    transformItems?: (items: SitemapItem[]) => Awaitable<SitemapItem[]>;
  };
  /**
   * Build hook, called once after the site is fully built - e.g. to
   * generate an RSS feed.
   */
  buildEnd?: (siteConfig: SiteConfig<ThemeConfig>) => Awaitable<void>;
  /**
   * Build hook, called after each page's SSG render - e.g. to handle
   * teleported content.
   */
  postRender?: (context: SSGContext) => Awaitable<SSGContext | void>;
  /**
   * Build hook for adding head entries that cannot be statically
   * declared, called before a page's HTML is written to disk. Returned
   * entries are merged with the existing ones.
   */
  transformHead?: (ctx: TransformContext<ThemeConfig>) => Awaitable<HeadConfig$1[] | void>;
  /**
   * Build hook for transforming a page's final HTML, called before it
   * is written to disk.
   */
  transformHtml?: (code: string, id: string, ctx: TransformContext<ThemeConfig>) => Awaitable<string | void>;
  /**
   * Hook for augmenting the data of each page, applied when rendering
   * markdown to vue in both dev and build. Returned values are merged
   * into the page data.
   */
  transformPageData?: (pageData: PageData, ctx: TransformPageContext<ThemeConfig>) => Awaitable<Partial<PageData> | {
    [key: string]: any;
  } | void>;
  /**
   * Multi-layer configuration overloading: additional configs that
   * apply to the pages in their directory and below, either as a dict
   * keyed by path or as a loader function.
   * Auto-resolves to `docs/.../config.{js,mjs,ts,mts}` when unspecified.
   *
   * Set to `{}` to opt-out.
   * @experimental
   */
  additionalConfig?: AdditionalConfigDict<ThemeConfig> | AdditionalConfigLoader<ThemeConfig>;
}
/**
 * The resolved config, as exposed to build hooks and plugins.
 */
interface SiteConfig<ThemeConfig = any> extends Pick<UserConfig<ThemeConfig>, 'markdown' | 'vue' | 'vite' | 'shouldPreload' | 'router' | 'mpa' | 'lastUpdated' | 'ignoreDeadLinks' | 'cleanUrls' | 'useWebFonts' | 'postRender' | 'buildEnd' | 'transformHead' | 'transformHtml' | 'transformPageData' | 'sitemap' | 'icons'> {
  /**
   * Absolute path of the project root (the directory containing
   * `.vitepress`).
   */
  root: string;
  /**
   * Absolute path of the directory containing the markdown sources.
   */
  srcDir: string;
  /**
   * Absolute path of the public assets directory. Empty string when
   * disabled through vite's `publicDir` option.
   */
  publicDir: string;
  /**
   * The resolved site data.
   */
  site: SiteData$2<ThemeConfig>;
  /**
   * Absolute path of the loaded config file, if any.
   */
  configPath: string | undefined;
  /**
   * Absolute paths of the files the config depends on, watched to
   * restart the dev server.
   */
  configDeps: string[];
  /**
   * Absolute path of the active theme's directory.
   */
  themeDir: string;
  /**
   * Absolute path of the build output directory.
   */
  outDir: string;
  /**
   * Directory for assets within the build output.
   */
  assetsDir: string;
  /**
   * URL prefix for built assets, normalized to end with a slash.
   */
  assetsBase?: string;
  /**
   * Absolute path of the cache directory.
   */
  cacheDir: string;
  /**
   * Absolute path of the temp directory used during the build.
   */
  tempDir: string;
  /**
   * Markdown source paths, relative to `srcDir`.
   */
  pages: string[];
  /**
   * The resolved dynamic routes.
   */
  dynamicRoutes: ResolvedRouteConfig[];
  /**
   * The resolved page rewrites: `map` goes from source path to
   * rewritten path, `inv` the other way around.
   */
  rewrites: {
    map: Record<string, string | undefined>;
    inv: Record<string, string | undefined>;
  };
  /**
   * The logger used by vite.
   */
  logger: Logger;
  /**
   * The raw user config this config was resolved from.
   */
  userConfig: UserConfig<ThemeConfig>;
  /**
   * Number of pages rendered concurrently during the build.
   */
  buildConcurrency: number;
}
//#endregion
//#region src/node/config.d.ts
declare function normalizeSiteBase(base?: string): string;
declare function normalizeAssetsBase(assetsBase: string): string;
type UserConfigFn<ThemeConfig> = (env: ConfigEnv) => Awaitable<UserConfig<ThemeConfig>>;
type UserConfigExport<ThemeConfig> = Awaitable<UserConfig<ThemeConfig>> | UserConfigFn<ThemeConfig>;
/**
 * Type config helper
 */
declare function defineConfig<ThemeConfig = DefaultTheme.Config>(config: UserConfig<NoInfer<ThemeConfig>>): UserConfig<NoInfer<ThemeConfig>>;
type AdditionalConfigFn<ThemeConfig> = (env: ConfigEnv) => Awaitable<AdditionalConfig<ThemeConfig>>;
type AdditionalConfigExport<ThemeConfig> = Awaitable<AdditionalConfig<ThemeConfig>> | AdditionalConfigFn<ThemeConfig>;
/**
 *  Type config helper for additional/locale-specific config
 */
declare function defineAdditionalConfig<ThemeConfig = DefaultTheme.Config>(config: AdditionalConfig<NoInfer<ThemeConfig>>): AdditionalConfig<NoInfer<ThemeConfig>>;
/**
 * Type config helper for custom theme config
 *
 * @deprecated use `defineConfig` instead
 */
declare function defineConfigWithTheme<ThemeConfig>(config: UserConfig<ThemeConfig>): UserConfig<ThemeConfig>;
declare function resolveConfig(root?: string, command?: 'serve' | 'build', mode?: string): Promise<SiteConfig>;
declare function isAdditionalConfigFile(path: string): boolean;
declare function resolveUserConfig(root: string, command: 'serve' | 'build', mode: string): Promise<[UserConfig, configPath: string | undefined, configDeps: string[]]>;
declare function mergeConfig<A extends object, B extends object>(a: A, b: B, isRoot?: boolean): A & B;
declare function resolveSiteData(root: string, userConfig?: UserConfig, command?: 'serve' | 'build', mode?: string): Promise<SiteData$2>;
//#endregion
//#region src/node/build/build.d.ts
declare function build(root?: string, buildOptions?: BuildOptions & {
  base?: string;
  assetsBase?: string;
  mpa?: string;
  onAfterConfigResolve?: (siteConfig: SiteConfig) => Awaitable<void>;
}): Promise<void>;
//#endregion
//#region src/node/contentLoader.d.ts
interface ContentOptions<T = ContentData[]> {
  /**
   * Include src?
   * @default false
   */
  includeSrc?: boolean;
  /**
   * Render src to HTML and include in data?
   * @default false
   */
  render?: boolean;
  /**
   * If `boolean`, whether to parse and include excerpt? (rendered as HTML)
   *
   * If `function`, control how the excerpt is extracted from the content.
   *
   * If `string`, define a custom separator to be used for extracting the
   * excerpt. Default separator is `---` if `excerpt` is `true`.
   *
   * @see https://github.com/jonschlinkert/gray-matter#optionsexcerpt
   * @see https://github.com/jonschlinkert/gray-matter#optionsexcerpt_separator
   *
   * @default false
   */
  excerpt?: boolean | ((file: {
    data: {
      [key: string]: any;
    };
    content: string;
    excerpt?: string;
  }, options?: any) => void) | string;
  /**
   * Transform the data. Note the data will be inlined as JSON in the client
   * bundle if imported from components or markdown files.
   */
  transform?: (data: ContentData[]) => Awaitable<T>;
  /**
   * Options to pass to `tinyglobby` and `picomatch` for globbing.
   */
  globOptions?: GlobOptions;
}
interface ContentData {
  url: string;
  src: string | undefined;
  html: string | undefined;
  frontmatter: Record<string, any>;
  excerpt: string | undefined;
}
/**
 * Create a loader object that can be directly used as the default export
 * of a data loader file.
 */
declare function createContentLoader<T = ContentData[]>(
/**
 * files to glob / watch - relative to srcDir
 */
watch: string | string[], options?: ContentOptions<T>): {
  watch: string[];
  options: {
    globOptions: GlobOptions | undefined;
  };
  load(files?: string[]): Promise<T>;
};
//#endregion
//#region src/node/init/init.d.ts
declare const ScaffoldThemeType: {
  readonly Default: "default theme";
  readonly DefaultCustom: "default theme + customization";
  readonly Custom: "custom theme";
};
type ScaffoldThemeType = (typeof ScaffoldThemeType)[keyof typeof ScaffoldThemeType];
interface ScaffoldOptions {
  root?: string;
  srcDir?: string;
  title?: string;
  description?: string;
  theme?: ScaffoldThemeType;
  useTs?: boolean;
  injectNpmScripts?: boolean;
  addNpmScriptsPrefix?: boolean;
  npmScriptsPrefix?: string;
}
declare function init(root?: string): Promise<void>;
declare function scaffold({ root: root_, srcDir: srcDir_, title, description, theme, useTs, injectNpmScripts, addNpmScriptsPrefix, npmScriptsPrefix }: ScaffoldOptions): Promise<string>;
//#endregion
//#region src/node/plugins/staticDataPlugin.d.ts
interface LoaderModule<T = any> {
  watch?: string[] | string;
  load: (watchedFiles: string[]) => Awaitable<T>;
  options?: {
    globOptions?: GlobOptions;
  };
}
/**
 * Helper for defining loaders with type inference
 */
declare function defineLoader<T>(loader: LoaderModule<T>): LoaderModule<T>;
//#endregion
//#region src/node/postcss/isolateStyles.d.ts
type Options$2 = {
  includeFiles?: RegExp[];
  ignoreFiles?: RegExp[];
  prefix?: string;
};
declare function postcssIsolateStyles({ includeFiles, ignoreFiles, prefix }?: Options$2): Plugin$1;
//#endregion
//#region node_modules/.pnpm/@polka+url@1.0.0-next.29/node_modules/@polka/url/index.d.ts
interface ParsedURL {
  pathname: string;
  search: string;
  query: Record<string, string | string[]> | undefined;
  hash: string | undefined;
  raw: string;
}
//#endregion
//#region node_modules/.pnpm/trouter@4.0.0/node_modules/trouter/index.d.ts
// Thank you: @fwilkerson, @stahlstift
// ---
/** @type {import('http').METHODS} */
type Methods = 'ACL' | 'BIND' | 'CHECKOUT' | 'CONNECT' | 'COPY' | 'DELETE' | 'GET' | 'HEAD' | 'LINK' | 'LOCK' | 'M-SEARCH' | 'MERGE' | 'MKACTIVITY' | 'MKCALENDAR' | 'MKCOL' | 'MOVE' | 'NOTIFY' | 'OPTIONS' | 'PATCH' | 'POST' | 'PRI' | 'PROPFIND' | 'PROPPATCH' | 'PURGE' | 'PUT' | 'REBIND' | 'REPORT' | 'SEARCH' | 'SOURCE' | 'SUBSCRIBE' | 'TRACE' | 'UNBIND' | 'UNLINK' | 'UNLOCK' | 'UNSUBSCRIBE';
type Pattern = RegExp | string;
declare class Trouter<T = Function> {
  find(method: Methods, url: string): {
    params: Record<string, string>;
    handlers: T[];
  };
  add(method: Methods, pattern: Pattern, ...handlers: T[]): this;
  use(pattern: Pattern, ...handlers: T[]): this;
  all(pattern: Pattern, ...handlers: T[]): this;
  get(pattern: Pattern, ...handlers: T[]): this;
  head(pattern: Pattern, ...handlers: T[]): this;
  patch(pattern: Pattern, ...handlers: T[]): this;
  options(pattern: Pattern, ...handlers: T[]): this;
  connect(pattern: Pattern, ...handlers: T[]): this;
  delete(pattern: Pattern, ...handlers: T[]): this;
  trace(pattern: Pattern, ...handlers: T[]): this;
  post(pattern: Pattern, ...handlers: T[]): this;
  put(pattern: Pattern, ...handlers: T[]): this;
}
//#endregion
//#region node_modules/.pnpm/polka@1.0.0-next.28/node_modules/polka/index.d.mts
type Promisable<T> = Promise<T> | T;
type ListenCallback = () => Promisable<void>;
interface IError extends Error {
  code?: number;
  status?: number;
  details?: any;
}
type NextHandler = (err?: string | IError) => Promisable<void>;
type ErrorHandler<T extends Request = Request> = (err: string | IError, req: T, res: Response, next: NextHandler) => Promisable<void>;
type Middleware<T extends IncomingMessage = Request> = (req: T & Request, res: Response, next: NextHandler) => Promisable<void>;
type Response = ServerResponse;
interface Request extends IncomingMessage {
  url: string;
  method: string;
  originalUrl: string;
  params: Record<string, string>;
  path: string;
  search: string;
  query: Record<string, string>;
  body?: any;
  _decoded?: true;
  _parsedUrl: ParsedURL;
}
interface Polka<T extends Request = Request> extends Trouter<Middleware<T>> {
  readonly server: Server;
  readonly wares: Middleware<T>[];
  readonly onError: ErrorHandler<T>;
  readonly onNoMatch: Middleware<T>;
  readonly handler: Middleware<T>;
  parse: (req: IncomingMessage) => ParsedURL;
  use(pattern: RegExp | string, ...handlers: (Polka<T> | Middleware<T>)[]): this;
  use(...handlers: (Polka<T> | Middleware<T>)[]): this;
  listen(port?: number, hostname?: string, backlog?: number, callback?: ListenCallback): this;
  listen(port?: number, hostname?: string, callback?: ListenCallback): this;
  listen(port?: number, backlog?: number, callback?: ListenCallback): this;
  listen(port?: number, callback?: ListenCallback): this;
  listen(path: string, backlog?: number, callback?: ListenCallback): this;
  listen(path: string, callback?: ListenCallback): this;
  listen(options: ListenOptions, callback?: ListenCallback): this;
  listen(handle: any, backlog?: number, callback?: ListenCallback): this;
  listen(handle: any, callback?: ListenCallback): this;
}
//#endregion
//#region src/node/serve/serve.d.ts
interface ServeOptions {
  base?: string;
  assetsBase?: string;
  root?: string;
  port?: number;
}
declare function serve(options?: ServeOptions): Promise<Polka<Request>>;
//#endregion
//#region src/node/server.d.ts
declare function createServer(root?: string // for backwards compatibility
, serverOptions?: ServerOptions & {
  base?: string;
}, restartServer?: () => Promise<void>, config?: SiteConfig): Promise<import("vite").ViteDevServer>;
//#endregion
//#region src/node/utils/getGitTimestamp.d.ts
declare function cacheAllGitTimestamps(root: string, pathspec?: string[]): Promise<void>;
declare function getGitTimestamp(file: string): Promise<number>;
//#endregion
export { AdditionalConfigExport, AdditionalConfigFn, type ConfigEnv, ContentData, ContentOptions, type DefaultTheme, type HeadConfig, type Header, type LoaderModule, type MarkdownLocaleOptions, MarkdownOptions, MarkdownRenderer, type Plugin, RawConfigExports, type ResolvedRouteConfig, type RouteModule, ScaffoldOptions, ScaffoldThemeType, ServeOptions, SiteConfig, type SiteData, ThemeOptions, TransformContext, TransformPageContext, UserConfig, UserConfigExport, UserConfigFn, build, cacheAllGitTimestamps, createContentLoader, createMarkdownRenderer, createServer, defineAdditionalConfig, defineConfig, defineConfigWithTheme, defineLoader, defineRoutes, disposeMdItInstance, getGitTimestamp, init, isAdditionalConfigFile, loadEnv, mergeConfig, mergeMarkdownLocales, normalizeAssetsBase, normalizeSiteBase, postcssIsolateStyles, resolveConfig, resolvePages, resolveSiteData, resolveSiteDataByRoute, resolveUserConfig, scaffold, serve };
## 数据加载功能确认（2026-09-19）
- 用户问是否用了 vitepress 官方"构建时数据加载"功能：有。`.vitepress/theme/posts.data.js` 用 createContentLoader('articles/**/*.md') 实现，被 HomeLayout.vue（首页卡片）、PostsList.vue（全部文章）、TagsPage.vue（标签页）三处导入。
- 修复笔误：`excerpts: true` → `excerpt: true`（官方选项名为 excerpt，原来摘录从未生效）。
- macOS/BSD grep 不支持 BRE 的 \| 交替，需用 grep -E；此前一度误判"无引用"。

## 迁移旧文档 frontmatter 适配（2026-09-19 上午）
- 用户导入旧 md/mdx 文档（健康健身/生活指南/置身事内/投资笔记，含 8 个 .mdx）。
- 批量补齐 frontmatter：date=2026-05-29(迁移日)、category=专题名、tags=[栏目名]；保留 title/description/order。
- autoSidebar.ts 排序升级：order 升序优先（手动编排），无 order 按日期倒序。
- 图片：35→42 张从 pureblog 拷到 public/images/，引用统一改写为 /images/xxx 根路径。
- 关键坑：VitePress 不支持 .mdx（需插件），8 个 mdx 已改名 .md；spacex.mdx 的 astro `import {Aside}` 移除，<Aside> 转 ::: tip 容器。
- basicindex.md 改名 health-indicators.md；start.md 错误 description 修正。

## 本地头像与 favicon（2026-09-19 08:08）
- 用户把 favicon 套件放入 public/favicon/（ico/32x32/apple-touch-icon/android-192/512）。
- HomeLayout.vue 头像由 GitHub 远程头像改为 withBase('/favicon/android-chrome-512x512.png')。
- config.mts head 增加 4 条 favicon link；构建验证通过。

## 第二批文章迁移（2026-09-19 上午续）
- 新增 31 篇（ai-tech 4、smart-life 1、zstn firm/gov/macro 8、notes 16、2 个 mdx 改名）。
- astro frontmatter → VitePress：publishDate→date(补零)、category/tags 按目录映射、order 接在既有 max 后按发布日期升序（firm 5-8、gov 3-4、macro 5-6、ai-tech 1-4、smart-life 5）；notes 不加 order（不在 autoSidebar 专题内）。
- heroImage 一律删除（主题不渲染）；4 篇本地 heroImage 的正文均已有图，无信息损失。
- 图片：28 张从 pureblog/src/assets/images 拷到 public/images（现 70 张）；正文 ../../assets/images 引用统一改写为 /images/。
- 遗留坏引用：iphone7plus.md 的 image4（C:/Users/wycpr/...）在 pureblog 也未迁移，已删该行，如找回原图可补 /images/image4.png。
- 清 .vitepress/cache 后 docs:build 通过，feed.xml 64 条。

## 专题与侧边栏同步（2026-09-19 08:40）
- config.mts：zstn 增加 law（法治观察）栏目；新增 notes 专题（随笔：日记/读书笔记/游记/建站笔记）。
- 删除已不存在目录的手动侧边栏与导航项（frontEnd/note/myProjects），nav 专题项改为纯 topics 自动生成。
- 补建 index.md：zstn/law、notes 根 + 4 个栏目；补转 jiya.md 旧 frontmatter（order 1，tags=[法治观察]）。
- 构建通过，dist/articles 下旧目录已清除。

## sidebarTitle 侧边栏短标题（2026-09-19 11:31）
- autoSidebar.ts：parseFrontmatter 增加 sidebarTitle 字段，条目显示名优先 sidebarTitle > title > slug。
- 15 篇长标题文章（>12字）批量加 sidebarTitle，如"怎么刺激消费和房价…"→"刺激消费的韩国样本"。
- 22 个栏目/专题 index.md 的提示语更新，说明 sidebarTitle 用法。构建验证通过。

## 导航栏毛玻璃 + 文章标题去横线（2026-09-19 16:57）
- 参照 blog.msglbo.cn 做玻璃导航：style.css 的 `:root`/`.dark` 设 `--vp-nav-bg-color`（亮 rgba(245,246,248,.72) / 暗 rgba(17,24,39,.6)）+ `--vp-nav-backdrop-filter: blur(12px) saturate(1.6)`。VitePress 2.0 alpha 原生支持这两个变量（navbar ::before 背景层），首页顶部仍全透明（--vp-nav-home-bg-color 默认 transparent）。`--vp-nav-divider-color` 调为半透明。
- ArticleHeader.vue 删除 .article-header 的 border-bottom/padding-bottom，去掉一级标题下横线。
- 验证：bun run docs:dev + agent-browser，getComputedStyle(.VPNavBar,'::before') 确认 blur 生效；取值需等 0.25s 过渡结束，否则读到旧值。
- agent-browser 未全局安装，装在 /Users/wyc/.workbuddy/binaries/node/workspace，用 PATH 前缀调用。
